import 'package:flutter/material.dart';

// Placeholder — puerto real de components/tabs/after-tab.tsx llega en la Fase 3.
// Sin Scaffold/AppBar propio — el shell provee uno solo.
class AfterTabScreen extends StatelessWidget {
  const AfterTabScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('After — Fase 3: grabaciones, historial de alertas'));
  }
}
