import 'package:flutter/material.dart';

// Placeholder — puerto real de components/tabs/before-tab.tsx llega en la Fase 3
// (ubicación en vivo, rutas, mapa). Sin Scaffold/AppBar propio — el shell provee uno solo.
class BeforeTabScreen extends StatelessWidget {
  const BeforeTabScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Before — Fase 3/5: ubicación en vivo, rutas, mapa'));
  }
}
