import 'package:flutter/material.dart';

// Placeholder — puerto real de components/tabs/during-tab.tsx llega en la Fase 2
// (núcleo del SOS: botón, grabación, RecorderController). Sin Scaffold/AppBar propio.
class DuringTabScreen extends StatelessWidget {
  const DuringTabScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('During — Fase 2: núcleo del SOS'));
  }
}
